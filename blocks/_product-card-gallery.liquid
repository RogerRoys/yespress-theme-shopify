{% assign product = closest.product %}

{% capture children %}
  {% unless product == blank %}
    <div
      class="product-badges product-badges--{{ settings.badge_position }}"
      style="
        --badge-border-radius: {{ settings.badge_corner_radius }}px;
        --badge-font-family: var(--font-{{ settings.badge_font_family | handle }}--family);
        --badge-font-weight: var(--font-{{ settings.badge_font_family | handle }}--weight);
        --badge-text-transform: {{ settings.badge_text_transform }};
      "
    >
      {%- if product.available == false or product.compare_at_price > product.price -%}
        <div
          class="
            product-badges__badge product-badges__badge--rectangle
            {% if product.available == false %}
              color-{{ settings.badge_sold_out_color_scheme }}
            {% elsif product.compare_at_price > product.price %}
              color-{{ settings.badge_sale_color_scheme }}
            {% endif %}
          "
        >
          {%- if product.available == false -%}
            {{ 'content.product_badge_sold_out' | t }}
          {%- elsif product.compare_at_price > product.price -%}
            {{ 'content.product_badge_sale' | t }}
          {%- endif -%}
        </div>
      {%- endif -%}

      {% if settings.enable_new_badge and product.metafields.custom["new-arrival"] != blank %}
        <div class="new_badge" style="
          padding-top: {{ settings.new_badge_padding_top }}px;
          padding-right: {{ settings.new_badge_padding_right }}px;
          padding-bottom: {{ settings.new_badge_padding_bottom }}px;
          padding-left: {{ settings.new_badge_padding_left }}px;
          border-radius: {{ settings.custom_border_radius }};
          margin-top: 5px;
          font-size: {{ settings.new_badge_font_size }}px;
          background-color: {{ settings.new_badge_bg_color }};
          color: {{ settings.new_badge_text_color }};
        ">
          {{ product.metafields.custom["new-arrival"] | default: "New Arrivals" }}
        </div>
      {% endif %}

      {% if settings.enable_best_badge and product.metafields.custom["best-selling"] != blank %}
        <div class="best" style="
          padding-top: {{ settings.new_badge_padding_top }}px;
          padding-right: {{ settings.new_badge_padding_right }}px;
          padding-bottom: {{ settings.new_badge_padding_bottom }}px;
          padding-left: {{ settings.new_badge_padding_left }}px;
          border-radius: {{ settings.custom_border_radius }};
          margin-top: 5px;
          font-size: {{ settings.best_badge_font_size }}px;
          background-color: {{ settings.best_badge_bg_color }};
          color: {{ settings.best_badge_text_color }};
        ">
          {{ product.metafields.custom["best-selling"] | default: "Best Selling" }}
        </div>
      {% endif %}

      {% if settings.enable_custom_badge and product.metafields.custom["custom-badge"] != blank %}
        <div class="custom" style="
          padding-top: {{ settings.new_badge_padding_top }}px;
          padding-right: {{ settings.new_badge_padding_right }}px;
          padding-bottom: {{ settings.new_badge_padding_bottom }}px;
          padding-left: {{ settings.new_badge_padding_left }}px;
          border-radius: {{ settings.custom_border_radius }};
          margin-top: 5px;
          font-size: {{ settings.custom_badge_font_size }}px;
          background-color: {{ settings.custom_badge_bg_color }};
          color: {{ settings.custom_badge_text_color }};
        ">
          {{ product.metafields.custom["custom-badge"] | default: "Limited Edition" }}
        </div>
      {% endif %}
    </div>

    {% if settings.quick_add or settings.mobile_quick_add %}
      {% render 'quick-add', product: product, section_id: section.id %}
    {% endif %}
  {% endunless %}
{% endcapture %}

{% render 'card-gallery', children: children %}

{%- comment -%} Title and price for the zoomed out grid view {%- endcomment -%}
<div class="product-grid-view-zoom-out--details">
  {% if product == blank %}
    <h3 class="h4">{{ 'content.product_card_placeholder' | t }}</h3>
  {% else %}
    <h3 class="h4">{{ product.title }}</h3>
  {% endif %}
</div>


{% stylesheet %}
  .product-badges {
    --badge-inset: max(var(--padding-xs), calc((var(--border-radius) + var(--padding-xs)) * (1 - cos(45deg))));

    position: absolute;
    z-index: var(--layer-flat);
  }

  .product-badges--bottom-left {
    bottom: calc(var(--badge-inset) + var(--padding-block-start));
    left: calc(var(--badge-inset) + var(--padding-inline-start));
  }

  .product-badges--top-left {
    top: calc(var(--badge-inset) + var(--padding-block-start));
    left: calc(var(--badge-inset) + var(--padding-inline-start));
  }

  .product-badges--top-right {
    top: calc(var(--badge-inset) + var(--padding-block-start));
    right: calc(var(--badge-inset) + var(--padding-inline-start));
  }

  .product-badges__badge {
    --badge-font-size: var(--font-size--xs);

    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--color-foreground);
    background: var(--color-background);
    font-size: var(--badge-font-size);
    font-family: var(--badge-font-family);
    font-weight: var(--badge-font-weight);
    text-transform: var(--badge-text-transform);
    border-radius: var(--badge-border-radius);
  }

  .product-badges__badge--rectangle {
    padding-block: var(--badge-rectangle-padding-block);
    padding-inline: var(--badge-rectangle-padding-inline);
  }
{% endstylesheet %}

<Style>
    .product-badges__badge--rectangle {
     font-size: {{ settings.sale_badge_font_size }}px !important;
  }
</Style>


<Style>
   .product-badges__badge--rectangle {
    padding-top: {{ settings.new_badge_padding_top }}px !important;
    padding-right: {{ settings.new_badge_padding_right }}px !important;
    padding-bottom: {{ settings.new_badge_padding_bottom }}px !important;
    padding-left: {{ settings.new_badge_padding_left }}px !important;
         margin-top: 5px;
     font-size: {{ settings.sale_badge_font_size }}px !important;
  }
</Style>

{% schema %}
{
  "name": "t:names.product_image",
  "tag": null,
  "settings": [
    {
      "type": "paragraph",
      "content": "t:content.resource_reference_product_media"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:options.auto"
        },
        {
          "value": "portrait",
          "label": "t:options.portrait"
        },
        {
          "value": "square",
          "label": "t:options.square"
        },
        {
          "value": "landscape",
          "label": "t:options.landscape"
        }
      ],
      "default": "portrait",
      "label": "t:settings.aspect_ratio",
      "info": "t:info.aspect_ratio_adjusted"
    },
    {
      "type": "header",
      "content": "t:content.borders"
    },
    {
      "type": "select",
      "id": "border",
      "label": "t:settings.style",
      "options": [
        {
          "value": "none",
          "label": "t:options.none"
        },
        {
          "value": "solid",
          "label": "t:options.solid"
        }
      ],
      "default": "none"
    },
    {
      "type": "range",
      "id": "border_width",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "t:settings.thickness",
      "default": 1,
      "visible_if": "{{ block.settings.border != 'none' }}"
    },
    {
      "type": "range",
      "id": "border_opacity",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "label": "t:settings.opacity",
      "default": 100,
      "visible_if": "{{ block.settings.border != 'none' }}"
    },
    {
      "type": "range",
      "id": "border_radius",
      "label": "t:settings.border_radius",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-start",
      "label": "t:settings.left",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-end",
      "label": "t:settings.right",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "t:names.product_card_media",
      "category": "t:categories.product"
    }
  ]
}
{% endschema %}
